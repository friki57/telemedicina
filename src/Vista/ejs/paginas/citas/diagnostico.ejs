<%- include("./../../partes/cabecera.ejs") %>

    <!-- Doctor - Posta
Fecha
hora
observacion fsdak fasdjl fkj
estado -->
    <script type="text/javascript">
        var roomID = "<%= datos.cita._id %>"
    </script>
    <script src="https://unpkg.com/peerjs@1.3.1/dist/peerjs.min.js" defer></script>
    <script src="https://cdnjs.cloudflare.com/ajax/libs/socket.io/3.0.4/socket.io.js"></script>

  <div class="d-flex justify-content-center" style="width:100%;">
    <div class="cont" style="background:rgb(255, 255, 255)">
      <h1 class="text-center" style="margin-bottom:20px; margin-top:20px;">Sala de video llamadas</h1>
      <div class="card" style="height:90%; margin-bottom:40px;">
        <div class="card-body">
          <p>Nombre del Doctor:<%=datos.cita.doctor.nombres%> <%=datos.cita.doctor.apellidos%></p>
          <% if(datos.cita.posta!=undefined){%>
            <p>Posta:<%=datos.cita.posta.nombre%></p>
            <p>Nombre Encargado Posta:<%=datos.cita.adminPosta.nombres%> <%=datos.cita.adminPosta.apellidos%></p>
          <%}%>
          <p class="card-text">Fecha y hora de la Cita: <%= datos.cita.fecha %>, <%= datos.cita.hora %><p>
            <p class="card-text">Temperatura: <%= datos.cita.temperatura %>
            </p>
            <p class="card-text">Presión arterial: <%= datos.cita.presion %>
            </p>
            <textarea name="observacion" class="form-control" placeholder="Leave a comment here" id="floatingTextarea2" style="height: 200px;" disabled="disabled">
<%= datos.cita.observacion%>
            </textarea>
            <p class="card-text">Ingrese Aquí el resultado del diagnostico.</p>
          <form action="<%= datos.http.post.diagnostico%>" method="post">
            <input type="text" name="idCita" value="<%= datos.cita._id %>" style="display:none;">
            <textarea name="diagnostico" class="form-control" id="" cols="30" rows="10"
                placeholder="Ingrese aquí el resultado del dignostico"  autofocus><%= datos.cita.diagnostico %>
            </textarea>
            <div class="form-control d-grid gap-2">
                <button type="submit" class="btn btn-success">Guardar
                Resultado</button>
            </div>
          </form>
    </div>

        </div>

        </div>
    </div>

    <%- include("./../../partes/piePagina.ejs") %>